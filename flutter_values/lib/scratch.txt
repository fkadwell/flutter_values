import 'package:flutter/material.dart';
void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        backgroundColor: Color.fromARGB(255, 5, 120, 70),
        body: Center(
          child: Text('Hello World!'),
        ),
      ),
    ),
  );
}


import 'package:flutter/material.dart';

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        backgroundColor: Color.fromARGB(255, 5, 120, 41),
        body: Center(
          child: Text('Hello World!'),
        ),
      ),
    ),
  );
}

import 'package:flutter/material.dart';
import 'package:flutter_values/gradient_container.dart';

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        body: GradientContainer(),
      ),
    ),
  );
}


child: Center(
        child: Image.asset('assets/images/dice-1.png')

child: Center(column(
  children: [Image.asset('dice-1.png', width: 200),
  TextButton(onPressed: onPress, child: const Text('Roll Die')
)]
))

In Column mainAxisSize: mainAxisSize.min

In the TextButton add style: TextButton.styleFrom(foreground: Blah, TextStyle(Font: Blah))

padding: EdgeInsets.only(top:...)

SizedBox

setState